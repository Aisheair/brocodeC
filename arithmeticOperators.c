#include <stdio.h>

int main(){
    //arithmetic operators

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ increment
    // -- decrement

    int x = 5;
    int y = 2;

    // int z  = x + z;
    // int z  = x - z;
    // int z  = x * z;
    // float z = x / (float) y;
    // int z = x  %  y;

    x++;
    y--;

    printf("%d",y);

    return 0;
}